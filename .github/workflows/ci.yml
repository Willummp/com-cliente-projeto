name: Pipeline CI/CD Completo (TP5)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  # JOB 1: Segurança, Build e Testes
  security-build-test:
    name: 🛡️ Segurança e Qualidade
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write
    
    steps:
      - name: Checkout do Código
        uses: actions/checkout@v4

      - name: Configurar Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      # --- MUDANÇA PRINCIPAL: CODEQL ---
      - name: 🛡️ Inicializar CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: java

      # Build acontece aqui
      - name: 🏗️ Build e Testes Unitários
        run: mvn clean verify
      
      - name: 🕵️‍♂️ Realizar Análise CodeQL
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:java"
      # ---------------------------------
      
      - name: 📝 Gerar Log de Testes
        if: always()
        run: |
          echo "=== RESUMO DA EXECUÇÃO ===" > resumo_execucao.log
          echo "Data: $(date)" >> resumo_execucao.log
          cat resumo_execucao.log

      - name: 📤 Upload Relatório JaCoCo
        uses: actions/upload-artifact@v4
        with:
          name: relatorio-cobertura
          path: target/site/jacoco/

      - name: 📦 Upload do JAR da Aplicação
        uses: actions/upload-artifact@v4
        with:
          name: app-jar
          path: target/*.jar

      - name: 📊 Resumo do Job
        run: |
          echo "### 🚀 Resultado do Build" >> $GITHUB_STEP_SUMMARY
          echo "✅ Testes executados com sucesso" >> $GITHUB_STEP_SUMMARY
          echo "🛡️ Análise CodeQL concluída" >> $GITHUB_STEP_SUMMARY

  # JOB 2: Deploy em Homologação
  deploy-homolog:
    name: 🚀 Deploy Homologação
    needs: security-build-test
    runs-on: ubuntu-latest
    
    steps:
      - name: Baixar Artefato
        uses: actions/download-artifact@v4
        with:
          name: app-jar
      - name: Simular Deploy
        run: echo "Aplicação implantada com sucesso em Homologação!"
      - name: 🧪 Smoke Test
        run: echo "Health Check OK!"

  # JOB 3: Deploy em Produção
  deploy-prod:
    name: 🚀 Deploy Produção
    needs: deploy-homolog
    runs-on: ubuntu-latest
    
    steps:
      - name: Baixar Artefato
        uses: actions/download-artifact@v4
        with:
          name: app-jar
      - name: Simular Deploy
        run: echo "✅ Deploy em Produção concluído!"